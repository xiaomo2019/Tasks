### JS的导入方式
- 内联样式(head和body都可)
  - JS代码放入`<script>`中
- 外联样式
  - 建立.js文件
  - `<script src="">`
***
### 变量声明和常量
- var
- let(推荐)
- const(常量)
***
### js条件语句
`if`,`else if`,`else`
***
### 函数使用
- 使用function定义函数